apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbo-collateral-service.fullname" . }}
  labels:
    {{- include "dbo-collateral-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbo-collateral-service.selectorLabels" . | nindent 4 }}
